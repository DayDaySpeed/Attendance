<Window x:Class="Attendance.View.StudentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="学生信息编辑"
        Height="360" Width="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">


    <!-- 主界面 -->
    <Border CornerRadius="16" Padding="20">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#AEE2FF" Offset="0.0"/>
                <GradientStop Color="#FFD6E8" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="学生信息编辑" FontSize="22" FontWeight="Bold"
                       Foreground="#333" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="学号：" Width="80" VerticalAlignment="Center" FontSize="15" Foreground="#444"/>
                <TextBox Text="{Binding StudentNumber}" Width="280" Style="{StaticResource RoundedTextBox}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="姓名：" Width="80" VerticalAlignment="Center" FontSize="15" Foreground="#444"/>
                <TextBox Text="{Binding Name}" Width="280" Style="{StaticResource RoundedTextBox}"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="性别：" Width="80" VerticalAlignment="Center" FontSize="15" Foreground="#444"/>
                <ComboBox SelectedItem="{Binding Gender}"
                            Width="280"
                            Height="34"
                            FontSize="14"
                            Padding="6"
                            Background="White"
                            BorderThickness="1"
                            VerticalContentAlignment="Center">
                    <ComboBox.ItemsSource>
                        <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                            <sys:String>男</sys:String>
                            <sys:String>女</sys:String>
                        </x:Array>
                    </ComboBox.ItemsSource>
                </ComboBox>






            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Style="{StaticResource RoundedGlowButton}" Content="确定" Command="{Binding ConfirmCommand}" Margin="10"/>
                <Button Style="{StaticResource RoundedGlowButton}" Content="取消" Background="#E91E63" Command="{Binding CancelCommand}" Margin="10"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
